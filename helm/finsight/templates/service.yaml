apiVersion: v1
kind: Service
metadata:
  name: {{ include "finsight.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "finsight.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "finsight.fullname" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
